---
- name: Build Docker Image in local host
  hosts: local
  vars:
    ansible_python_interpreter: /usr/bin/python3  # Example path to Python 3 interpreter
  tasks:
  # become: yes
  roles:
    - DockerBuild

# - name: deploy docker and kubernetes to remote host
#   hosts: all
#   become: yes
#   roles:
#     - Deploy
 
 
