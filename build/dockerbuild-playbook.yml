---
- name: Build Docker Image in local host
  hosts: local
  # Example path to Python 3 interpreter
  tasks:
  # become: yes
  roles:
    - Deploy

# - name: deploy docker and kubernetes to remote host
#   hosts: all
#   become: yes
#   roles:
#     - Deploy
 
 
